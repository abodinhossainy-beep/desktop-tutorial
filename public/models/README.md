# 3D product models

Put production GLB/GLTF assets here.

Expected environment variable:

`VITE_BEDROOM_MODEL_URL=/models/bedroom-set.glb`

For best performance, export the furniture model as GLB, use separate material names for wood/fabric/metal, and compress geometry with Draco where appropriate.
