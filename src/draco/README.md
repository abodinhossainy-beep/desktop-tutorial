# Draco decoder assets

Place the Draco decoder files used by Three.js in this directory when the production build is prepared.

The application currently passes `/draco/` to `useGLTF`. Keep decoder assets local in production to avoid depending on a third-party CDN.

Recommended source: the official KhronosGroup Draco project referenced by `Fasani/three-js-resources`.
